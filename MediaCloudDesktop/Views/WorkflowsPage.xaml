<Page x:Class="MediaCloudDesktop.Views.WorkflowsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="工作流管理">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 标题和创建按钮 -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="工作流管理" 
                     FontSize="24" 
                     FontWeight="Bold" 
                     VerticalAlignment="Center"
                     Foreground="#303133"/>
            
            <Button x:Name="CreateWorkflowButton" 
                  Content="创建工作流" 
                  Grid.Column="1"
                  Style="{StaticResource ModernButton}"
                  Click="CreateWorkflowButton_Click"/>
        </Grid>

        <!-- 搜索 -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchTextBox" 
                   Grid.Column="0"
                   Style="{StaticResource ModernTextBox}"
                   Margin="0,0,10,0"
                   TextChanged="SearchTextBox_TextChanged"
                   ToolTip="搜索工作流名称"/>

            <Button x:Name="RefreshButton" 
                  Content="刷新" 
                  Grid.Column="2"
                  Style="{StaticResource ModernButton}"
                  Click="RefreshButton_Click"/>
        </Grid>

        <!-- 工作流列表 -->
        <DataGrid x:Name="WorkflowsDataGrid" 
                Grid.Row="2"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                SelectionMode="Single"
                SelectionChanged="WorkflowsDataGrid_SelectionChanged"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                CanUserReorderColumns="True"
                CanUserResizeColumns="True"
                CanUserResizeRows="False"
                CanUserSortColumns="True"
                GridLinesVisibility="Horizontal"
                HeadersVisibility="Column"
                AlternatingRowBackground="#F5F7FA">
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" 
                                  Binding="{Binding Id}" 
                                  Width="60"/>
                <DataGridTextColumn Header="工作流名称" 
                                  Binding="{Binding Name}" 
                                  Width="150"/>
                <DataGridTextColumn Header="描述" 
                                  Binding="{Binding Description}" 
                                  Width="200"/>
                <DataGridTextColumn Header="类型" 
                                  Binding="{Binding Type}" 
                                  Width="80"/>
                <DataGridTemplateColumn Header="颜色" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding Color}" 
                                  Width="20" 
                                  Height="20" 
                                  CornerRadius="2"
                                  Margin="5"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridCheckBoxColumn Header="激活" 
                                     Binding="{Binding IsActive}" 
                                     Width="60"/>
                <DataGridTextColumn Header="状态" 
                                  Binding="{Binding Status}" 
                                  Width="80"/>
                <DataGridTextColumn Header="创建者" 
                                  Binding="{Binding Creator.Username}" 
                                  Width="100"/>
                <DataGridTextColumn Header="创建时间" 
                                  Binding="{Binding CreatedAt, StringFormat='yyyy-MM-dd HH:mm'}" 
                                  Width="150"/>
                <DataGridTextColumn Header="成员数" 
                                  Binding="{Binding Members.Count}" 
                                  Width="80"/>
                <DataGridTemplateColumn Header="操作" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="查看" 
                                      Style="{StaticResource ModernButton}"
                                      Background="#409EFF"
                                      Margin="0,0,5,0"
                                      Click="ViewWorkflow_Click"/>
                                <Button Content="编辑" 
                                      Style="{StaticResource ModernButton}"
                                      Background="#67C23A"
                                      Margin="0,0,5,0"
                                      Click="EditWorkflow_Click"/>
                                <Button Content="删除" 
                                      Style="{StaticResource ModernButton}"
                                      Background="#F56C6C"
                                      Click="DeleteWorkflow_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- 分页控件 -->
        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1" 
                      Orientation="Horizontal" 
                      VerticalAlignment="Center">
                <Button x:Name="PrevPageButton" 
                      Content="上一页" 
                      Style="{StaticResource ModernButton}"
                      Click="PrevPageButton_Click"
                      Margin="0,0,10,0"/>
                
                <TextBlock x:Name="PageInfoTextBlock" 
                         Text="第 1 页，共 1 页" 
                         VerticalAlignment="Center" 
                         Margin="0,0,10,0"
                         Foreground="#606266"/>
                
                <Button x:Name="NextPageButton" 
                      Content="下一页" 
                      Style="{StaticResource ModernButton}"
                      Click="NextPageButton_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
