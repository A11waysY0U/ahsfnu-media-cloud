<Page x:Class="MediaCloudDesktop.Views.TagsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="标签管理">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 标题和创建按钮 -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="标签管理" 
                     FontSize="24" 
                     FontWeight="Bold" 
                     VerticalAlignment="Center"
                     Foreground="#303133"/>
            
            <Button x:Name="CreateTagButton" 
                  Content="创建标签" 
                  Grid.Column="1"
                  Style="{StaticResource ModernButton}"
                  Click="CreateTagButton_Click"/>
        </Grid>

        <!-- 标签列表 -->
        <DataGrid x:Name="TagsDataGrid" 
                Grid.Row="2"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                SelectionMode="Single"
                SelectionChanged="TagsDataGrid_SelectionChanged"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                CanUserReorderColumns="True"
                CanUserResizeColumns="True"
                CanUserResizeRows="False"
                CanUserSortColumns="True"
                GridLinesVisibility="Horizontal"
                HeadersVisibility="Column"
                AlternatingRowBackground="#F5F7FA">
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" 
                                  Binding="{Binding Id}" 
                                  Width="60"/>
                <DataGridTextColumn Header="标签名称" 
                                  Binding="{Binding Name}" 
                                  Width="150"/>
                <DataGridTemplateColumn Header="颜色" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Background="{Binding Color}" 
                                  Width="20" 
                                  Height="20" 
                                  CornerRadius="2"
                                  Margin="5"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="创建者" 
                                  Binding="{Binding Creator.Username}" 
                                  Width="100"/>
                <DataGridTextColumn Header="创建时间" 
                                  Binding="{Binding CreatedAt, StringFormat='yyyy-MM-dd HH:mm'}" 
                                  Width="150"/>
                <DataGridTextColumn Header="关联素材数" 
                                  Binding="{Binding MaterialTags.Count}" 
                                  Width="100"/>
                <DataGridTemplateColumn Header="操作" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="编辑" 
                                      Style="{StaticResource ModernButton}"
                                      Background="#67C23A"
                                      Margin="0,0,5,0"
                                      Click="EditTag_Click"/>
                                <Button Content="删除" 
                                      Style="{StaticResource ModernButton}"
                                      Background="#F56C6C"
                                      Click="DeleteTag_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
