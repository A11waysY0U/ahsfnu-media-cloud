<Page x:Class="MediaCloudDesktop.Views.InviteCodesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="邀请码管理">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="邀请码管理" FontSize="24" FontWeight="Bold" Foreground="#303133"/>
            <Button x:Name="GenerateButton" Content="生成邀请码" Grid.Column="1"
                    Style="{StaticResource ModernButton}" Click="GenerateButton_Click"/>
        </Grid>
        <DataGrid x:Name="InviteCodesDataGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True"
                  CanUserAddRows="False" CanUserDeleteRows="False" AlternatingRowBackground="#F5F7FA">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="邀请码" Binding="{Binding Code}" Width="200"/>
                <DataGridTextColumn Header="状态" Binding="{Binding Status}" Width="80"/>
                <DataGridTextColumn Header="创建者" Binding="{Binding CreatedBy}" Width="80"/>
                <DataGridTextColumn Header="使用者" Binding="{Binding UsedBy}" Width="80"/>
                <DataGridTextColumn Header="创建时间" Binding="{Binding CreatedAt, StringFormat='yyyy-MM-dd HH:mm'}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
